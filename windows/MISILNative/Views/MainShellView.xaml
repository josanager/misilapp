<UserControl x:Class="MISILNative.Views.MainShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{StaticResource BrushBgPrimary}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" MinWidth="220" MaxWidth="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar Navigation -->
        <Border Grid.Column="0" Background="{StaticResource BrushBgSecondary}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Brand Header -->
                <Grid Grid.Row="0" Margin="16,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="30" Height="30" Background="{StaticResource BrushAccent}" CornerRadius="8">
                            <TextBlock Text="M" FontWeight="Bold" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="MISIL" FontSize="17" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="10,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Grid>

                <!-- Navigation Links -->
                <StackPanel Grid.Row="1" Margin="10,8">
                    <!-- Nav Chats -->
                    <Border x:Name="NavChats" CornerRadius="8" Padding="12,10" Margin="0,2" Cursor="Hand" Background="{StaticResource BrushBgActive}" MouseLeftButtonUp="OnNavChatsClick">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💬" FontSize="14" Foreground="{StaticResource BrushAccent}" Margin="0,0,12,0" />
                            <TextBlock Text="Chats" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>

                    <!-- Nav Dashboard -->
                    <Border x:Name="NavDashboard" CornerRadius="8" Padding="12,10" Margin="0,2" Cursor="Hand" Background="Transparent" MouseLeftButtonUp="OnNavDashboardClick">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💽" FontSize="14" Foreground="{StaticResource BrushTextSecondary}" Margin="0,0,12,0" />
                            <TextBlock Text="Dashboard" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextSecondary}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>

                    <!-- Nav Settings -->
                    <Border x:Name="NavSettings" CornerRadius="8" Padding="12,10" Margin="0,2" Cursor="Hand" Background="Transparent" MouseLeftButtonUp="OnNavSettingsClick">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙️" FontSize="14" Foreground="{StaticResource BrushTextSecondary}" Margin="0,0,12,0" />
                            <TextBlock Text="Ajustes" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextSecondary}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Node Status Footer -->
                <Border Grid.Row="2" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,1,0,0" Padding="16,14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Width="10" Height="10" CornerRadius="5" VerticalAlignment="Center" Margin="0,0,12,0" Background="{StaticResource BrushAccent}" x:Name="NodeStatusDot" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtNodeStatusTitle" Text="Nodo activo" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" />
                            <TextBlock x:Name="TxtNodeStatusDetail" Text="0 B compartidos" FontSize="11" Foreground="{StaticResource BrushTextMuted}" Margin="0,1,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- Right Content Host -->
        <Grid Grid.Column="1" x:Name="ContentHost" />
    </Grid>
</UserControl>

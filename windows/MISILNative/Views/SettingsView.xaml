<UserControl x:Class="MISILNative.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800"
             Background="{StaticResource BrushBgPrimary}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,1" Padding="24,0">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Ajustes" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" />
                <TextBlock Text="Configuración de este equipo Windows" FontSize="11" Foreground="{StaticResource BrushTextMuted}" Margin="0,1,0,0" />
            </StackPanel>
        </Border>

        <!-- Body -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="28">
            <StackPanel MaxWidth="700" HorizontalAlignment="Left">
                
                <!-- Section 1: Storage -->
                <TextBlock Text="Almacenamiento" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,10" />
                <Border Style="{StaticResource MISILCard}" Margin="0,0,0,24">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Aportación del nodo" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" />
                            <TextBlock x:Name="TxtQuotaStatus" Text="No estás compartiendo espacio" FontSize="12" Foreground="{StaticResource BrushTextMuted}" Margin="0,3,0,0" />
                        </StackPanel>
                        <Button x:Name="BtnChangeQuota" Grid.Column="1" Content="Configurar" Style="{StaticResource MISILSecondaryButton}" Height="34" Padding="14,0" FontSize="12" Click="OnChangeQuotaClick" />
                    </Grid>
                </Border>

                <!-- Section 2: Network -->
                <TextBlock Text="Red" FontFamily="{StaticResource FontHeadline}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,10" />
                <Border Style="{StaticResource MISILCard}" Margin="0,0,0,24">
                    <StackPanel>
                        <Grid Margin="0,4">
                            <StackPanel>
                                <TextBlock Text="Presencia en Internet" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" />
                                <TextBlock x:Name="TxtNetworkSettingStatus" Text="Comprobando conexión" FontSize="12" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0" />
                            </StackPanel>
                            <Ellipse x:Name="NetworkSettingDot" Width="9" Height="9" Fill="{StaticResource BrushTextMuted}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                        </Grid>
                        <Rectangle Height="1" Fill="{StaticResource BrushBorder}" Margin="0,8" />
                        <Grid Margin="0,4">
                            <StackPanel>
                                <TextBlock Text="Relay de capacidad" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" />
                                <TextBlock Text="https://misil-web.pages.dev" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0" />
                            </StackPanel>
                            <TextBlock Text="10 s" FontFamily="{StaticResource FontLabel}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource BrushTextSecondary}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Section 3: Privacy -->
                <TextBlock Text="Privacidad y Seguridad" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,10" />
                <Border Style="{StaticResource MISILCard}" Margin="0,0,0,24">
                    <StackPanel>
                        <Grid Margin="0,4">
                            <StackPanel>
                                <TextBlock Text="Perfil local" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" />
                                <TextBlock Text="No existe una cuenta ni una sesión en servidores remotos." FontSize="12" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0" />
                            </StackPanel>
                            <TextBlock Text="✓" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushAccent}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                        </Grid>
                        <Rectangle Height="1" Fill="{StaticResource BrushBorder}" Margin="0,8" />
                        <Grid Margin="0,4">
                            <StackPanel>
                                <TextBlock Text="Clave del nodo" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" />
                                <TextBlock Text="Protegida mediante Windows Data Protection API (DPAPI)." FontSize="12" Foreground="{StaticResource BrushTextMuted}" Margin="0,2,0,0" />
                            </StackPanel>
                            <TextBlock Text="✓" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushAccent}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Section 4: Testing / Reset -->
                <TextBlock Text="Pruebas" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,10" />
                <Border Style="{StaticResource MISILCard}" Margin="0,0,0,24">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Repetir configuración inicial" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource BrushTextPrimary}" />
                            <TextBlock Text="Borra la preferencia de inicio, pero conserva los archivos y mensajes." FontSize="12" Foreground="{StaticResource BrushTextMuted}" Margin="0,3,0,0" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Restablecer" Style="{StaticResource MISILSecondaryButton}" Height="34" Padding="14,0" FontSize="12" Click="OnResetOnboardingClick" />
                    </Grid>
                </Border>

                <!-- Version Info -->
                <TextBlock Text="MISIL Local Alpha 0.2.0 · Windows (x64)" FontSize="12" Foreground="{StaticResource BrushTextMuted}" Margin="0,10,0,0" />

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
